# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.key

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# local configuration and runtime caches
/utils/session-validator.js
/pages/api/session-sync.js
/vpn/*.json
/vpn/usv2.ovpn

# SSH keys and PEM certificates (bulletproof lock)
/bot_key
/bot_key.pub
/*.pem
/ai_key.pem
/fixed_ai_key.pem

# Deployment archives (VPS upload bundles - never commit these)
*.tar.gz
*.zip
*.tar

# Agent folder (rules, notes - all private)
/.agents/

# Cloudflare Origin Certificates folder
/certs/

# Grafana dashboard JSON files (downloaded by monitoring/setup.sh — do not commit)
/monitoring/grafana/dashboards/*.json

# Local development override for monitoring stack (Windows Docker Desktop only)
docker-compose.monitoring.override.yml
